export default (state=[],action)=>{
    if(action.type === 'CREATE'){
        switch (action.type) {
            case 'FETCH_ALL':
                return state;
            case 'CRETATE':
                return state;
        
            default:
                return state;
        }
    }
}